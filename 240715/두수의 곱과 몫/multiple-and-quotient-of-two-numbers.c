#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    int a, b, c, d;
    scanf("%d %d", &a, &b);
    c = a * b;
    d = a / b;
    printf("%d * %d = %d\n%d / %d = %d", a, b, c, a, b, d);
    return 0;
}